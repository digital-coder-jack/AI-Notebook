# =====================================================================
#  Study Sphere AI  -  Python dependencies
#  Powers BOTH the Telegram bot and the full web application.
# =====================================================================

# --- Web framework + server -----------------------------------------
fastapi==0.115.8
uvicorn==0.34.0
python-multipart==0.0.27      # required by FastAPI for file uploads / forms

# --- Telegram bot (unchanged) ---------------------------------------
python-telegram-bot==21.10

# --- HTTP client used to call the Groq API --------------------------
httpx==0.28.1

# --- Data validation (FastAPI request models) -----------------------
pydantic==2.10.6

# --- File text extraction for the upload feature --------------------
pypdf==6.12.0                  # PDF text extraction
python-docx==1.1.2            # DOCX text extraction

# NOTE: Password hashing and JWT are implemented with the Python
# standard library (hashlib / hmac), so no extra crypto packages are
# needed and the app deploys cleanly on Vercel's Python runtime.
