# Public base URL of the Study Sphere backend API.
# This is exposed to the browser (NEXT_PUBLIC_*) and must point ONLY
# to the backend. Provider keys are NEVER referenced here.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api
