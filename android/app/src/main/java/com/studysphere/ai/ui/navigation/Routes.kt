package com.studysphere.ai.ui.navigation

/**
 * Centralized navigation route constants to avoid typos and keep
 * navigation consistent across the app.
 */
object Routes {
    const val LOGIN = "login"
    const val REGISTER = "register"
    const val CHAT = "chat"
    const val HISTORY = "history"
    const val SETTINGS = "settings"
    const val PROFILE = "profile"
    const val MODEL_SELECTION = "model_selection"
}
